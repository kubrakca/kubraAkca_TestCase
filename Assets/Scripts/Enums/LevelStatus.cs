namespace Enums
{
    public enum LevelStatus
    {
        #region Values

        Locked,     // Locked
        Active,     // NextLevel
        Completed   // CompletedLevel

        #endregion
    }
}
